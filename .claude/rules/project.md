# Architecture Decisions

These are non-obvious conventions that can't be inferred from code alone.

**Package manager**: `uv` (not pip/poetry). Run everything via `uv run`.

**Python version**: 3.14 (pinned in `.python-version`).

## Environment Variables
- `DJANGO_SECRET_KEY` — Django secret key (required in production, has insecure fallback for dev)
- `DEBUG` — `"1"` enables debug mode (default), `"0"` for production
- `ALLOWED_HOSTS` — Comma-separated hostnames (only used when `DEBUG=0`)
- `CSRF_TRUSTED_ORIGINS` — Comma-separated origins incl. scheme, e.g. `https://app.example.com` (only used when `DEBUG=0`)
- `REDIS_URL` — Connection string for `CACHES['default']` (Django's built-in `django.core.cache.backends.redis.RedisCache`), e.g. `redis://localhost:6379/0`. **Required when `LLM_API_KEY` is set** — `ai.E001` blocks an ineffective cache backend (LocMem / FileBased / Dummy) once AI is enabled, independent of `DEBUG`. Recommended for any production-shaped deploy (atomic cross-worker rate-limit counters via Redis `INCR` + CalDAV event-cache perf). When unset, `CACHES['default']` falls back to `LocMemCache` — fine only when AI is disabled. A `KEY_PREFIX` of `dayforge` namespaces all keys (`ai_cmd_rl:*`, `caldav_events:*`) so a co-tenanted Redis won't collide.
- `LLM_API_KEY` — OpenAI-compatible API key for the AI command bar. When empty, AI endpoints return 503 and the frontend shows a degraded-mode indicator; manual editing still works.
- `LLM_BASE_URL` — OpenAI-compatible base URL. Default `https://api.openai.com/v1`. Set to OpenRouter or a self-hosted proxy to swap providers.
- `LLM_MODEL` — Model name passed to the chat-completions API. Default `gpt-4o-mini`.
- `LLM_REQUEST_TIMEOUT` — Hard timeout for the LLM HTTP call in seconds. Default `15`. Prevents a hung provider from holding a worker.
- `LLM_MAX_COMMAND_CHARS` — Cap on the user's command string before sending to the LLM. Default `500`.
- `LLM_RATE_LIMIT_PER_HOUR` — Per-user fixed-window rate limit on `POST /api/ai/schedules/<date>/command/`. Default `100`. Counter lives in `CACHES['default']` under `ai_cmd_rl:<user_id>`; a 429 is returned when exceeded. **Set `REDIS_URL` when AI is enabled — Redis is the production-shaped default** and gives atomic, cross-worker counters (Redis `INCR`). The Django system check `ai.E001` (see `backend/ai/checks.py`) fires whenever `LLM_API_KEY` is set and `CACHES['default']` is an ineffective cache backend for rate limiting (LocMem / Dummy are per-process; FileBased is non-atomic across workers), **independent of `DEBUG`**, so an AI-enabled deploy cannot boot on such a backend.
- `LLM_DRAFT_MODEL` — Model used for draft generation (`POST /api/ai/schedules/<date>/generate-draft/`). Default `gpt-4o`. Heavier than `LLM_MODEL` because drafts shape a whole day from history (PRD §15.3).
- `LLM_DRAFT_RATE_LIMIT_PER_HOUR` — Independent fixed-window counter (`ai_draft_rl`) for the draft endpoint. Default `10`. Same `REDIS_URL`-when-AI-enabled requirement as `LLM_RATE_LIMIT_PER_HOUR` — covered by the same `ai.E001` system check.
- `LLM_HISTORY_DAYS` — Number of past schedules included in the draft context. Default `7` (PRD §6.2). Only schedules with `status` in `{active, reviewed}` are included — `draft`-status days are excluded so the AI doesn't train on its own unreviewed output.
- `LLM_DRAFT_CAPTURE_PROMPT_PATH` — Test-only. **Overwrites the target file on every `generate-draft` request when set.** Set to a writable file path (e.g. `/tmp/draft_prompt_test7.txt`) to capture the rendered LLM draft user_message. Used by `frontend/scripts/playwright/draft-prompt-history-suffix.mjs` to verify the prompt content end-to-end without patching `service.py` by hand. Default empty (disables capture). The Django system check `ai.E002` (see `backend/ai/checks.py`) blocks startup when this is set while `DEBUG=False`, so a misconfigured production deploy fails loudly instead of silently spooling the user's full schedule history (PII) to disk.
- `LLM_CHAT_RATE_LIMIT_PER_HOUR` — Independent fixed-window counter (`ai_chat_rl`) for the multi-turn chat endpoint (`POST /api/ai/schedules/<date>/chat/`, feature 0007). Default `60`. Same `REDIS_URL`-when-AI-enabled requirement as the other AI buckets — covered by the same `ai.E001` system check (references `ai_cmd_rl`, `ai_draft_rl`, `ai_chat_rl`).
- `LLM_CHAT_MAX_TURNS` — Hard cap on `len(messages[])` per chat request. Default `40`. Protects against runaway transcript size if the client never resets the thread.
- `LLM_CHAT_MAX_TOTAL_CHARS` — Cap on the sum of `content` across all chat messages in one request. Default `LLM_MAX_COMMAND_CHARS × 8` (= `4000` with the default 500). Caps prompt cost regardless of how many short turns the user piles on.
- `LLM_CHAT_MAX_ASK_CHARS` — Schema cap on the model's clarifying-question field. Default `300`. Truncated/runaway responses surface as a 502 parse error, not a UI overflow.
- `LLM_MAX_EXPLANATION_CHARS` — Canonical schema cap on the model's `explanation` field. Default `300`. Shared by both the one-shot `validate_response_envelope` (legacy) and the chat envelope (feature 0007). Tightened from the previous hardcoded 500 so both surfaces stay aligned; the system prompt asks for one-sentence explanations.
- `ANALYTICS_STREAK_THRESHOLD` — Per-day completion ratio required for a day to count toward the streak. Default `0.8`. Must be a float in `[0.0, 1.0]`; out-of-range values raise `ValueError` at import time so a misconfigured deploy fails loudly instead of silently producing `streak=0` forever.
- `ANALYTICS_STREAK_WINDOW_DAYS` — Hard cap on how far back the streak query walks. Default `30`. Must be a positive int; `<= 0` raises `ValueError` at import time. Prevents `O(account-age)` scans on long-lived accounts.
- `CALDAV_ENCRYPTION_KEY` — Fernet symmetric key (URL-safe base64, 32 bytes) used to encrypt CalDAV passwords at rest in `calendar_sync.CalDAVAccount.password_encrypted`. Generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`. No insecure default — empty value is tolerated only in `DEBUG=True`; `calendar_sync.E001` blocks `DEBUG=False` startup when unset.
- `CALDAV_DEFAULT_BASE_URL` — Default base URL for the CalDAV connect form. Default `https://caldav.icloud.com/`. Echoed back by `GET /api/calendar/account/` (`default_base_url` field) so the frontend never hardcodes the value; changing it ships without a frontend rebuild.
- `CALDAV_REQUEST_TIMEOUT` — Hard timeout (seconds) on every CalDAV HTTP call. Default `10`. Same risk profile as `LLM_REQUEST_TIMEOUT` — prevents a hung iCloud connection from holding a worker.
- `CALDAV_CACHE_TTL_SECONDS` — Per-(user, date) event-list cache window (`calendar_sync/cache.py`). Default `300`. The cache key embeds `account.updated_at.isoformat()`, so credential rotation invalidates entries across all dates without any explicit `cache.delete` call. Non-shared cache backends (LocMem / FileBased / Dummy) are flagged by `calendar_sync.W001` as a perf concern (not a correctness one — versioned keys keep correctness intact regardless of backend). Setting `REDIS_URL` (required once AI is enabled) moves these keys into shared Redis and silences `calendar_sync.W001`.
- `TODOIST_ENCRYPTION_KEY` — Fernet symmetric key (URL-safe base64, 32 bytes) used to encrypt the Todoist personal API token at rest in `todoist_sync.TodoistAccount.token_encrypted`. Generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`. No insecure default — empty value is tolerated only in `DEBUG=True`; `todoist_sync.E001` blocks `DEBUG=False` startup when unset or malformed. (Feature 0020.)
- `TODOIST_BASE_URL` — API host for the Todoist REST client. Default `https://api.todoist.com/api/v1` — the **real API host**, not the `developer.todoist.com` docs host (requests there 404/return HTML and surface as a confusing `TodoistProviderError` 502). Analog of `CALDAV_DEFAULT_BASE_URL` — `service.py` builds every request URL from this base, so a self-hosted proxy ships without a code change.
- `TODOIST_REQUEST_TIMEOUT` — Hard timeout (seconds) on every Todoist HTTP call. Default `10`. Same risk profile as `CALDAV_REQUEST_TIMEOUT` — prevents a hung connection from holding a worker. `<= 0` raises `ValueError` at import time.
- `TODOIST_CACHE_TTL_SECONDS` — Per-(user, date) task-list cache window (`todoist_sync/cache.py`). Default `300`. The cache key embeds `account.updated_at.isoformat()`, so token rotation invalidates entries across all dates without any explicit `cache.delete` call. Non-shared cache backends are flagged by `todoist_sync.W001` as a perf concern (not correctness — versioned keys keep correctness intact). Setting `REDIS_URL` moves the `todoist_tasks:*` keys into shared Redis (they share `CACHES['default']`) and silences `todoist_sync.W001`. `<= 0` raises `ValueError` at import time.
- `HABITICA_ENCRYPTION_KEY` — Fernet symmetric key used to encrypt Habitica API tokens at rest in `habitica_sync.HabiticaAccount.api_token_encrypted`. Empty is tolerated only in `DEBUG=True`; `habitica_sync.E001` blocks `DEBUG=False` startup when unset or malformed. Generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`.
- `HABITICA_CLIENT_ID` — Maintainer Habitica user ID used in the required `x-client` header as `{HABITICA_CLIENT_ID}-DayForge`. No default; `habitica_sync.E001` blocks `DEBUG=False` startup when unset.
- `HABITICA_BASE_URL` — Habitica API base URL. Default `https://habitica.com/api/v3` (no trailing slash).
- `HABITICA_REQUEST_TIMEOUT` — Hard timeout (seconds) on every Habitica HTTP call. Default `10`; `<= 0` raises `ValueError`.
- `HABITICA_CACHE_TTL_SECONDS` — Per-(user, date, scope) Habitica task-list cache window (`habitica_sync/cache.py`). Default `300`; `<= 0` raises `ValueError`. The cache key embeds `account.updated_at.isoformat()`, so token rotation invalidates entries across all dates without any explicit `cache.delete` call. Non-shared cache backends are flagged by `habitica_sync.W001` as a perf concern (not correctness — versioned keys keep correctness intact). Setting `REDIS_URL` moves the `habitica_tasks:*` keys into shared Redis and silences `habitica_sync.W001`. (Feature 0024 / issue #73.)
- `EXTERNAL_TASKS_POLL_INTERVAL_SECONDS` — Background refresh interval for the unified external-tasks sidebar. Default `60`; `0` disables. **Do not lower without re-checking provider rate limits**: the poll forces `refresh=1` (server-cache bypass) and Habitica costs two upstream calls per tick (`todos` + `dailys`), so the interval directly divides Habitica's ~30 req/min per-user budget across open tabs — the 0024 plan's original `10` yielded 12 calls/min/tab and there is no client-side backoff after a 429. Passed to the frontend as `external_tasks_poll_interval`; `useExternalTasksPoll` calls every connected source's silent `refreshTasks`. Pauses while the browser tab is hidden. `< 0` raises `ValueError`.
- `GOOGLE_OAUTH_CLIENT_ID` / `GOOGLE_OAUTH_CLIENT_SECRET` / `GOOGLE_OAUTH_REDIRECT_URI` — OAuth 2.0 **Web-application** client credentials + server-side callback URL (e.g. `https://dayforge.habitreward.org/api/calendar/google/callback/`) from the Google Cloud console. The secret is **never logged**; the redirect URI is **server-config only** (the callback never trusts a client-supplied redirect). All default `""`. **Hard prod boot dependency**: `gcal_sync.E001` blocks `DEBUG=False` startup if any is unset **even when no user has connected Google** (the divergence from `calendar_sync.E001`, which only covers the encryption key) — an optional-Google staging env must set all four `GOOGLE_OAUTH_*` vars or stay `DEBUG=True`. (Feature 0022 / issue #31.)
- `GOOGLE_OAUTH_TOKEN_KEY` — Fernet symmetric key (URL-safe base64, 32 bytes) encrypting Google refresh **and** access tokens at rest in `gcal_sync.GoogleCalendarAccount`. Generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`. No insecure default — empty tolerated only in `DEBUG=True`; `gcal_sync.E001` blocks `DEBUG=False` startup when unset or malformed (validated via `Fernet(key)`).
- `GOOGLE_CALENDAR_API_BASE` — Calendar REST base URL. Default `https://www.googleapis.com/calendar/v3`. Analog of `CALDAV_DEFAULT_BASE_URL` — `service.py` builds every request URL from this, so a proxy ships without a code change.
- `GOOGLE_OAUTH_TOKEN_URI` / `GOOGLE_OAUTH_AUTH_URI` — token + authorization endpoints. Defaults `https://oauth2.googleapis.com/token` and `https://accounts.google.com/o/oauth2/v2/auth`.
- `GOOGLE_OAUTH_SCOPE` — space-separated read-only scopes. Default `https://www.googleapis.com/auth/calendar.readonly openid https://www.googleapis.com/auth/userinfo.email`. `calendar.readonly` covers `calendarList` enumeration + events read and **cannot write**; `openid` + `userinfo.email` make Google return a verifiable `id_token` carrying `sub` + `email` (account identity). **Parsed internally with `.split()`** so each scope flows separately through `Flow.from_client_config(..., scopes=...)`. Request the **canonical** scope strings (not the short `email` alias) so the requested set equals Google's returned set and `oauthlib` doesn't raise `Scope has changed`. `build_authorization_url` deliberately omits `include_granted_scopes` (no incremental auth → Google can't widen the returned set), and `gcal_sync.service` sets `OAUTHLIB_RELAX_TOKEN_SCOPE=1` (via `os.environ.setdefault`) as belt-and-suspenders so a superset returned scope on re-consent (a user with other grants on the same client) never crashes `fetch_token`.
- `GOOGLE_REQUEST_TIMEOUT` — float, default `10`. Hard cap on every Google HTTP call (mirror `CALDAV_REQUEST_TIMEOUT`). `<= 0` raises `ValueError` at import time.
- `GOOGLE_CACHE_TTL_SECONDS` — int, default `300`. Per-(user, account, date) event-list cache window (`gcal_sync/cache.py`). Versioned keys (key embeds `account.id` + `account.updated_at.isoformat()`) keep correctness regardless of backend; `gcal_sync.W001` warns on a non-shared backend. `<= 0` raises `ValueError` at import time.

**Google Calendar token rotation note (feature 0022)**: rotating `GOOGLE_OAUTH_TOKEN_KEY` invalidates all stored `GoogleCalendarAccount.refresh_token_encrypted` / `access_token_encrypted` ciphertext. The next `GET /api/calendar/google/events/<date>/` per affected user surfaces a `500 {"errors": {"detail": "Google Calendar service is misconfigured. Contact the administrator."}}` (the events view short-circuits the whole response on `ImproperlyConfigured` — a server-wide key problem, not per-account). Recovery: re-encrypt rows `Fernet(old)`→`Fernet(new)`, or instruct users to reconnect via Settings (`DELETE /api/calendar/google/accounts/<id>/` then re-run Connect). `gcal_sync.E001` validates only the *current* key. **Multi-account divergence**: unlike CalDAV/Todoist (one row per user, `OneToOne`), `GoogleCalendarAccount` is **multi-row** (`ForeignKey` + `UniqueConstraint(user, google_account_id)`); reconnecting the same Google account **upserts** the existing row. **No AI provider-egress**: Google event titles are display-only, never fed to draft/command/chat prompts.

**CalDAV key rotation note (feature 0011)**: rotating `CALDAV_ENCRYPTION_KEY` invalidates all stored `CalDAVAccount.password_encrypted` ciphertext. The next `GET /api/calendar/events/<date>/` per affected user surfaces a `500 {"errors": {"detail": "Calendar service is misconfigured..."}}` (the view's explicit `ImproperlyConfigured` catch — see `backend/calendar_sync/views.py:events`). Recovery options: (1) keep the old key accessible and re-encrypt rows with a one-off management command that loads with `Fernet(old)` and saves with `Fernet(new)`; (2) easier for small deployments — instruct all users to reconnect via Settings (`DELETE /api/calendar/account/` then `POST` with current credentials). The `calendar_sync.E001` startup check only validates the *current* key — it cannot detect mismatched ciphertext from a previous key.

**Todoist token rotation note (features 0020, 0021)**: rotating `TODOIST_ENCRYPTION_KEY` invalidates all stored `TodoistAccount.token_encrypted` ciphertext. The next `GET /api/todoist/tasks/<date>/` (or `POST /api/todoist/tasks/<id>/complete/`) per affected user surfaces a `500 {"errors": {"detail": "Todoist service is misconfigured. Contact the administrator."}}` (the view's explicit `ImproperlyConfigured` catch — mirrors the CalDAV one). Recovery: re-encrypt rows with `Fernet(old)`→`Fernet(new)`, or instruct users to reconnect via Settings (`DELETE /api/todoist/account/` then `POST` with the current token). The `todoist_sync.E001` startup check only validates the *current* key. **Write surface (feature 0021)**: `POST /api/todoist/tasks/<id>/complete/` completes a task — it is the **second** token-decryption call site (after `fetch_tasks_for_date`), still service-boundary-owned (`complete_task` lives in `service.py`; views pass the `TodoistAccount` through and never touch plaintext). A successful complete invalidates the user's task cache by bumping `account.updated_at` (`cache.invalidate_tasks` → plain `account.save()`; same `auto_now` footgun as `set_token`). `GET /api/todoist/tasks/<date>/?refresh=1` force-refetches bypassing the read cache (still re-warms it). **No AI provider-egress**: task text is never fed into draft/command/chat prompts — display + complete only, never sent to the LLM provider.

**Habitica token rotation note (feature 0024)**: rotating `HABITICA_ENCRYPTION_KEY` invalidates all stored `HabiticaAccount.api_token_encrypted` ciphertext. The next `GET /api/habitica/tasks/<date>/` or `POST /api/habitica/tasks/<id>/complete/` surfaces a config-shaped 500. Recovery: re-encrypt rows with `Fernet(old)`→`Fernet(new)`, or instruct users to reconnect via Settings (`DELETE /api/habitica/account/` then `POST`). Habitica task text is display-only and never sent to draft/command/chat prompts. Habitica's daily-list query value is spelled `dailys`; dailies show only in the client-today/overdue-carry scope and only when Habitica marks `isDue`.

**Performance note (draft history)**: PR #15 added `select_related("daily_review")` to the draft-history query in `ai_generate_draft` (`backend/ai/views.py`). `prompts.build_draft_user_message` reads `past.daily_review` for each past schedule to embed the per-day completion ratio in the LLM prompt; without the `select_related`, that's an N+1 — one extra query per past day. This fix predates the multi-turn chat feature (Phase-6 leftover) and applies regardless of whether chat is enabled.

**Privacy note**: User commands submitted via the AI endpoint are logged verbatim to the `AIInteraction` table for audit purposes (PRD §6.5). Users should avoid entering sensitive data (passwords, API keys) in command prompts.

**Privacy note (chat — feature 0007)**: The multi-turn chat endpoint (`POST /api/ai/schedules/<date>/chat/`) re-sends the **full prior client-supplied transcript** to the LLM provider on every turn — not just the latest message. This is a strictly larger provider-egress surface than the one-shot endpoint, even though the per-row DB log only stores the latest user turn (plus a `transcript_sha256` fingerprint of the whole transcript, see `backend/ai/views.py:_build_chat_audit_response`). Users should clear the thread (Clear button or page reload — thread state is in-memory per tab, no server-side persistence) before discussing anything sensitive. The thread is also auto-reset on date navigation (see `frontend/src/composables/useChat.ts:setActiveDate`).

**Privacy note (chat — wire-format detail)**: prior client-supplied turns are NOT forwarded to the provider under the privileged `assistant` role. They are flattened into a single user-role message prefixed with an "Untrusted prior transcript" header (see `backend/ai/service.py:run_chat` and `backend/ai/prompts.py:serialise_prior_turns`) so a tampered client cannot inject privileged assistant pre-commitments that bias the model toward destructive actions. The security invariant is regression-tested by `backend/tests/test_ai_service_chat.py::TestUntrustedTranscript::test_assistant_role_never_forwarded`.
