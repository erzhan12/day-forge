# 0084 — Resolve AI category labels and localised forms to slugs (issue #209)

## Context

In multi-turn chat, naming a category in the user's own language fails the whole
turn with **HTTP 502** and shows an internal validation string to the user:

```
AI chat response failed action validation:
action[0]: changes.category must be one of ['health', 'other', 'personal', 'work']
```

Repro (`frontend/scripts/playwright/ai-chat-clarifying-question.mjs`, real provider):
turn 1 `запланируй встречу` → block created; turn 2 `в 14:00 на час, рабочая` → 502.
The model puts the user's own word (`рабочая`) into `changes.category` instead of
the slug `work`. The English turn `at 14:00 for an hour, work` → 200.

Root causes (verified):

- `backend/ai/schemas.py:154-158` (update `changes.category`) and `:251-255`
  (top-level `category`) accept only an **exact** slug. There is no
  slug/label normalisation anywhere in `backend/ai/`, so even the label `Work`
  is rejected.
- `backend/ai/service.py:392-405` (`run_chat`) and `:275-285` (`run_draft`) raise
  `AIParseError` if **any** action fails, dropping the whole turn/draft.
- `backend/ai/views.py:79` maps `AIParseError` → 502, and `:1208` (chat) / `:969`
  (draft) return `{"errors": {"detail": str(e)}}`. The frontend shows `detail`
  verbatim (`frontend/src/utils/errorMessage.ts:8`).
- `backend/ai/prompts.py:108-115`: the chat `update` description lists
  `category` but never says it must be the **slug**. Hard rule 4 (`:159`, draft
  `:256`) renders `slug (label)` pairs via `_category_text` (`:43-45`), which
  invites label or free-text output.

## Decisions (agreed)

1. **Resolution:** exact slug → case-insensitive slug or label → otherwise unresolved.
2. **Unresolved on `add`** (chat and draft): fall back to the sink slug. This
   matches Hard rule 4's "Default to `other` if unclear".
3. **Unresolved on `update`:** drop `category` from `changes` and apply the rest.
   If `category` was the only change, drop the action. In **both** cases chat
   **asks which category was meant**, so the drop is never silent. Never
   quietly recategorise an existing block to the sink.
4. **Scope:** chat **and** draft, through one shared helper.
5. Remaining genuine `AIParseError`s (bad JSON, bad times, unknown keys) keep
   **502**. The provider did return malformed output, so 502 Bad Gateway stays.
   Only the user-facing message becomes generic; the detail goes to the audit row.

## Changes

### 1. New module `backend/ai/category_resolution.py`

- `resolve_category(value, categories) -> str | None`
  - `categories`: list of `(slug, label)` pairs. When the caller has no
    per-user catalog, use the `prompts._DEFAULT_CATEGORIES` pairs.
  - Non-`str` value → `None`. Leave it to schema validation, which still rejects
    it as malformed.
  - Step 1: `value` in slugs → return it unchanged.
  - Step 2: `key = value.strip().casefold()`. Return the first slug whose
    `slug.casefold()` or `label.strip().casefold()` equals `key`. At most one
    can match per user: slugs are unique per user, and labels are unique
    case-insensitively per user (`unique_user_category_label_ci`,
    `backend/schedules/models.py:190`). For the `_DEFAULT_CATEGORIES` fallback,
    first match in order wins.
  - Otherwise `None`. No fuzzy matching, stemming or translation: inflected
    forms like `рабочая` are handled by the prompt (§3), and the fallback below
    covers the model misbehaving anyway.
- `normalize_action_categories(actions, categories, sink_slug, known_task_ids) -> NormalizeResult`
  - A pure function. Returns a **new** list (never mutates the input list or
    its dicts) plus a tuple of
    `UnresolvedCategory(original_index, task_id, value, dropped: bool)`
    records.
  - Leave untouched: entries that are not dicts, and actions whose category is
    present but not a `str`. They keep failing validation exactly as today.
  - `add`:
    - `category` = `resolve_category(...)`, or `sink_slug` if unresolved.
    - An absent `category` key also gets `sink_slug`, per Hard rule 4's
      "Default to `other` if unclear". This is the only case where the key
      is added rather than rewritten.
    - Add fallbacks are never recorded: they are expected behaviour, not
      something to ask about.
  - `update` whose `changes` is a plain dict with a `str` `category`:
    - **Resolved:** rewrite it to the slug.
    - **Unresolved, other changes remain:** remove the key and keep the
      action. Record `UnresolvedCategory(..., dropped=False)`, so the user is
      told (§4) instead of the block being quietly retimed with its old
      category.
    - **Unresolved, `category` was the only change:** drop the action from
      the returned list and record `UnresolvedCategory(..., dropped=True)`.
      Drop only when the action would otherwise be valid. Guard: run
      `validate_action_shape(action, allowed | {value}, allow_untimed_add=True)`
      on the original action; it must return no errors, and `task_id` must be
      in `known_task_ids`. If either check fails, leave the action untouched
      so it fails validation (502) as today. A malformed action or unknown
      block never turns into a 200 question.
  - Other action types carry no category and pass through as-is.
- Logging: `logger.debug` only, with action type, `task_id` and outcome
  (`rewritten` / `sink` / `dropped`). **Never log the unresolved value**: it is
  the user's own wording, and the audit row's `raw` already holds it.

### 2. `backend/ai/service.py`

- Shared: build `category_pairs` and `sink_slug` once per call, exactly as the
  prompt call already does (`:248`, `:340`):
  `[(c.slug, c.label) for c in (categories or [])] or _DEFAULT_CATEGORIES`, and
  the `is_sink` slug or `"other"`. The helper must use the **same** fallback
  the prompt renders, so an empty list uses the defaults. The existing
  `allowed_categories` `is not None` check (`:275`, `:388`) is unchanged.
  `ordered_categories` never returns `[]`, and `sink_category_uses_other_slug`
  (`models.py:202`) guarantees the sink slug is always `other`.
- `run_chat`: after `validate_chat_response_envelope` passes (`:381-385`) and
  before the per-action loop (`:392`), run `normalize_action_categories` on
  `parsed["actions"]`, with `known_task_ids = {b.id for b in blocks}`.
  Validate and return the **normalised** list.
- `AIChatResult` (`:102`): add `unresolved_categories: tuple = ()` (default keeps
  the positional construction in the tests, e.g.
  `AIChatResult("{}", [...], "Changed", None)`, working).
- `run_draft`: normalise before `validate_draft_response` (`:280`). Guard with
  `isinstance(parsed, dict) and isinstance(parsed.get("actions"), list)` so
  envelope errors still come from the validator. Pass the normalised actions
  through to `AIDraftResult`. Drafts are add-only, so `unresolved` is always
  empty (`known_task_ids = set()`).
- Normalising before validation means `raw_response_text` keeps the model's
  original word for the audit row, while `parsed_actions`/`actions_json` and the
  apply path see only slugs.
- Invariant: if every action was an update dropped for its category, `actions`
  becomes `[]`, `ask` is `null` and `unresolved_categories` is non-empty. The
  view handles this (§4) before its chit-chat branch. The envelope rule "ask is
  null when actions is non-empty" is unaffected: a non-empty model `actions`
  already implies `ask` was `null`.
- Index coherence: every downstream consumer indexes the **normalised** list.
  That includes `find_replayed_actions`, `_apply_actions_sync` with its
  live-catalog category recheck (`views.py:789-801`), `plan_mutations`,
  `create_titles` and `actions_json`, so `action_index` stays consistent. Only
  the synthetic outcomes in §4 use `original_index`, and they are emitted only
  when the normalised list is empty, so the two index spaces never mix.

### 3. `backend/ai/prompts.py`

- Chat `update` description (`:108-113`): add one sentence. `changes.category`
  must be a category **slug** from Hard rule 4 — never the label or the user's
  own wording, whatever language the user writes in (e.g. a user saying
  "рабочая" means slug `work`).
- Chat Hard rule 4 (`:159`) and draft rule 4 (`:256`): state that the value
  emitted is the **slug** (the part before the parentheses), not the label.
  Keep `_category_text`'s `slug (label)` rendering; the label is what lets the
  model map the user's wording.

### 4. `backend/ai/views.py`

- `ai_chat`, after the audit log (`:1213`): if `result.unresolved_categories`,
  build a server-owned ask. It is English-only, like `_build_resolution_ask` /
  `build_guard_ask`, per RULES.md.
  `Which category should "<title>" use: <Label>, <Label>, …?`
  - Title comes from the blocks by `task_id`. A dropped record's `task_id` is
    known (§1 guard). A kept record's is validated by the planner, which
    returns 400 on an unknown id before any ask is built.
    - Use the post-apply `block_titles` (apply branch) or the pre-apply
      `current_blocks` (nothing-applied branch), with fallback `"that block"`
      as `_build_resolution_ask` does.
    - Use the first record; truncate the title like
      `replay_guard._GUARD_TITLE_TRUNCATE`.
  - Labels come from `schedule._categories` in catalog order.
  - Put the helper next to `_build_resolution_ask` as
    `_build_category_ask(unresolved, block_titles, categories)`.
  - Add a module constant `UNRESOLVED_CATEGORY_REASON_CODE =
    "unresolved_category"`, the counterpart of `REPLAY_GUARD_REASON_CODE`
    (`replay_guard.py:44`).
- Wiring:
  - **Nothing left to apply** (`parsed_actions == []` and every record has
    `dropped=True`). Place this branch after the `result.ask is not None`
    branch and before chit-chat. Mirroring `_replay_guard_response`
    (`views.py:333-379`):
    - build one synthetic
      `ActionOutcome(action_index=r.original_index, task_id=r.task_id,
      status="skipped", skipped_fields=("category",),
      reason_code=UNRESOLVED_CATEGORY_REASON_CODE)` per record;
    - pass them to `_mark_success(interaction, outcome_payload)` so
      `outcomes_json` records the skip;
    - return `blocks: None, applied: False, partial: False,
      ask: <category ask>, outcomes: <those>, explanation: result.explanation`.
  - **Actions applied:** the normal apply path. Afterwards, compute
    `ask = _build_resolution_ask(...) or _build_category_ask(...)`. The
    time-conflict question wins because the user can answer only one.
    - This covers the issue #209 repro exactly: the time change applies, and
      the response is `applied: true` plus the category ask. That
      partial-apply shape is already documented in `docs/api.md:388-391` and
      handled by `useChat.ts:233-260`.
    - The outcomes stay the planner's. No synthetic entries are added here,
      to keep the normalised index space clean (§2).
- The replay guard (`find_replayed_actions`) runs on the normalised list,
  unchanged.
- Generic parse message: in the `AIParseError` branch of **both** `ai_chat`
  (`:1196-1208`) and `ai_generate_draft` (`:956-969`), return a fixed detail
  (module constant, e.g. `AI_PARSE_ERROR_DETAIL = "The assistant's reply
  couldn't be applied. Try rephrasing."`) with status 502. All other `AIError`
  subclasses keep `str(e)` as today.
- Keep the validation detail for debugging:
  - Chat: `_build_chat_audit_response` (`views.py:1090`) gains an optional
    keyword `error_detail: str | None = None`. `_log_chat_failure` passes
    `str(exc)` for `AIParseError` only.
    - Insert `"error_detail"` into the payload **before** `"raw"`. The row is
      sliced to `_MAX_AI_RESPONSE_LOG_LEN` (`:82`, `:207`), so anything after
      a huge `raw` would be cut off.
    - Cap it at 2,000 characters, which bounds a 20-action error list.
    - Success rows and non-parse failures omit the key, so existing
      payload-shape assertions still hold.
  - Draft: `logger.warning("AI draft parse failure: %s", e)` before returning.
    The draft audit row keeps storing `raw`, as today.
    - This string never contains user wording: validation errors name the
      allowed slugs, not the rejected value.

### 5. No frontend change

- `useChat.ts:305` already shows `errors.detail`. It renders `ask` on a 200,
  including alongside `applied: true` (`useChat.ts:233-260`).
- The draft UI uses the same `extractErrorMessage`.
- No Playwright script asserts on the 502 `detail`. The only `errors.detail`
  assertion is `ai-draft-409-on-non-empty.mjs:198`, on a 409, which is
  unaffected.

### 6. Docs

- `docs/api.md`:
  - `:473` (chat 502) and `:526` (draft 502): `detail` is now the fixed
    generic string. The validation detail goes to the audit row
    (`error_detail`, chat) or the server log (draft).
  - Add a **"Success — unresolved category — `200 OK`"** subsection next to the
    replay-guard variant (`:408-443`), with an example body and the
    applied-plus-ask variant.
  - Add `unresolved_category` to the `reason_code` list (`:383`).
  - Update the envelope-uniformity note (`:461`): the replay guard is no
    longer the *only* non-apply case with non-empty `outcomes`.
- `RULES.md:164`: the server-owned English strings note becomes "the
  **fourth** such string", naming the category ask. Add one bullet: AI
  category values are resolved to slugs in `ai/category_resolution.py` before
  schema validation. Unresolved values fall back to the sink on add, and on
  update are dropped plus asked about; they are never quietly recategorised.

## Tests (write the failing ones first)

- `backend/tests/test_ai_category_resolution.py` (new):
  - `resolve_category`: exact slug; `Work`/`WORK`/` work ` → `work`; label match on
    a custom Russian label (`("work", "Работа")`, value `работа`) → `work`;
    `рабочая` → `None`; non-str → `None`.
  - `normalize_action_categories`:
    - **add:** unresolved → sink; absent `category` → sink; no record either way.
    - **update resolved:** rewritten to the slug.
    - **update unresolved with other changes:** category key removed, action
      kept, record with `dropped=False`.
    - **update category-only unresolved:** action removed, record with
      `dropped=True` and the correct `original_index`.
    - **S4 guard:** category-only unresolved updates are kept untouched (no
      record) when `task_id` is `"abc"`, has an unknown top-level key, or is
      not in `known_task_ids`.
    - The input list and its dicts are not mutated.
    - Non-dict entries and non-str categories pass through unchanged.
- `backend/tests/test_ai_service_chat.py`:
  - **The issue #209 repro.** A mocked provider returns
    `{"type":"update","task_id":N,"changes":{"start_time":"14:00","end_time":"15:00","category":"рабочая"}}`
    → no `AIParseError`. `parsed_actions[0]["changes"]` has no `category` and
    keeps the times; one `dropped=False` record.
  - A category-only `рабочая` update → `parsed_actions == []` and one
    `dropped=True` record.
  - The label `Work` → `work`.
  - A category-only `рабочая` update with an unknown `task_id` → still
    raises `AIParseError`.
- `backend/tests/test_ai_service_draft.py`: an add with label `Health` → `health`;
  an add with an unknown word → the sink slug; the draft no longer raises.
- `backend/tests/test_ai_views_chat.py`:
  - **The update with times + `рабочая`:** 200, block retimed, category
    unchanged, `applied: true`, and `ask` names the block and lists the labels.
  - **A resolution ask outranks it:** with the same update plus a conflicting
    time, the resolution ask wins.
  - **Category-only unresolved:** 200, `applied: false`, `ask` lists the
    labels, and `outcomes == [{..., "status": "skipped",
    "reason_code": "unresolved_category", "skipped_fields": ["category"]}]`.
    The audit row has `success=True` and those `outcomes_json`.
  - **An `AIParseError` response:** 502, `detail == AI_PARSE_ERROR_DETAIL`
    (not `str(e)`), and the audit payload has `error_detail` placed before
    `raw`.
    - Update `test_failure_envelope_carries_error_class` to match.
    - Extend `test_log_truncates_oversized_response` to assert `error_detail`
      survives truncation.
- `backend/tests/test_ai_views_draft.py`: an `AIParseError` → 502 with the generic detail.
- `backend/tests/test_ai_prompts_command_chat.py` / `test_ai_prompts_draft.py`:
  assert the new slug-not-label sentences are present.
- `backend/tests/test_ai_schemas_draft.py:115` (`test_update_invalid_category_rejected`)
  stays: the schema layer is unchanged and still rejects raw invalid values.

## Verification

- `uv run pytest backend/tests/ -v`, `uv run ruff check backend/`.
- Real provider: `make e2e-chat`. `ai-chat-clarifying-question.mjs` turn 2
  (`в 14:00 на час, рабочая`) should return 200.

## Risks

- **Slug/label cross-match:** a value could equal one category's slug
  case-insensitively and another's label. That can't happen with an exact
  slug (step 1 wins first); for the case-folded step, catalog order decides.
  Categories number at most 8 (`MAX_CATEGORIES`) and are user-curated, so this
  is accepted.
- **Sink fallback on add:** it may file a clearly-intended category under
  `other`. This matches existing Hard rule 4 behaviour; the prompt fix (§3)
  is the main lever.
- **English-only server ask:** the category ask is English-only, consistent with
  every other server-built ask. Localising server asks is out of scope.
